<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.SupplierMapper">

    <resultMap id="SupplierResult" type="com.ruoyi.system.domain.Supplier">
        <id     property="id"          column="id"/>
        <result property="name"        column="name"/>
        <result property="contact"     column="contact"/>
        <result property="phone"       column="phone"/>
        <result property="address"     column="address"/>
        <result property="category"    column="category"/>
        <result property="taxNo"       column="tax_no"/>
        <result property="bankAccount" column="bank_account"/>
        <result property="bankName"    column="bank_name"/>
        <result property="status"      column="status"/>
        <result property="remark"      column="remark"/>
        <result property="createBy"    column="create_by"/>
        <result property="createTime"  column="create_time"/>
        <result property="updateBy"    column="update_by"/>
        <result property="updateTime"  column="update_time"/>
    </resultMap>

    <sql id="selectSupplierVo">
        SELECT id, name, contact, phone, address, category, tax_no, bank_account, bank_name,
               status, remark, create_by, create_time, update_by, update_time
        FROM supplier WHERE del_flag = '0'
    </sql>

    <select id="selectSupplierList" resultMap="SupplierResult">
        <include refid="selectSupplierVo"/>
        <where>
            <if test="name != null and name != ''">AND name LIKE CONCAT('%', #{name}, '%')</if>
            <if test="category != null and category != ''">AND category = #{category}</if>
            <if test="status != null and status != ''">AND status = #{status}</if>
        </where>
        ORDER BY create_time DESC
    </select>

    <select id="selectSupplierById" resultMap="SupplierResult">
        <include refid="selectSupplierVo"/>
        AND id = #{id}
    </select>

    <insert id="insertSupplier">
        INSERT INTO supplier(id, name, contact, phone, address, category, tax_no, bank_account,
                             bank_name, status, remark, create_by, create_time)
        VALUES(#{id}, #{name}, #{contact}, #{phone}, #{address}, #{category}, #{taxNo},
               #{bankAccount}, #{bankName}, #{status}, #{remark}, #{createBy}, NOW())
    </insert>

    <update id="updateSupplier">
        UPDATE supplier
        <set>
            <if test="name != null and name != ''">name = #{name},</if>
            <if test="contact != null">contact = #{contact},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="address != null">address = #{address},</if>
            <if test="category != null">category = #{category},</if>
            <if test="taxNo != null">tax_no = #{taxNo},</if>
            <if test="bankAccount != null">bank_account = #{bankAccount},</if>
            <if test="bankName != null">bank_name = #{bankName},</if>
            <if test="status != null">status = #{status},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            update_time = NOW()
        </set>
        WHERE id = #{id}
    </update>

    <update id="deleteSupplierById">
        UPDATE supplier SET del_flag='2', update_time=NOW() WHERE id = #{id}
    </update>

    <update id="deleteSupplierByIds">
        UPDATE supplier SET del_flag='2', update_time=NOW()
        WHERE id IN
        <foreach collection="array" item="id" open="(" separator="," close=")">#{id}</foreach>
    </update>
</mapper>
